<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net6.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<GeneratePackageOnBuild>True</GeneratePackageOnBuild>
		<Title>The Lazy Crazy Brain Api Pack - Xml Tools</Title>
		<Description>Provides extended Xml functionality (eg. serializing and deserializing Interface properties, Complex Data Types etc.)</Description>
		<Authors>Jodie Watson</Authors>
		<Copyright>Copyright (c) 2022 Jodie Watson</Copyright>
		<PackageOutputPath>C:\LocalNugets\LazyApiPack\$(OutputPath)</PackageOutputPath>
		<AssemblyVersion>0.1.5</AssemblyVersion>
		<FileVersion>0.1.5</FileVersion>
		<Version>$(AssemblyVersion)</Version>
		<PackageProjectUrl>https://github.com/thelazycrazybrain/LazyApiPack.XmlTools</PackageProjectUrl>
		<PackageReadmeFile>ReadMe.md</PackageReadmeFile>
		<RepositoryUrl>https://github.com/thelazycrazybrain/LazyApiPack.XmlTools.git</RepositoryUrl>
		<PackageTags>xml;serializer;deserializer;recursive;nested;tool;wpf</PackageTags>
		<PackageReleaseNotes>
Introduces support to deserialize a list of classes directly.
Adds support to deserialize a property of a type that does not exactly match the object its holding (eg. the property class type is used as a base class but not marked as abstract).
</PackageReleaseNotes>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
	</PropertyGroup>

	<ItemGroup>
	  <None Update="ReadMe.md" Pack="True" PackagePath="\"/>
	</ItemGroup>

</Project>
